Need: python3.12+, Linux Environment prob
    [note python versions have a cpython bug aout c recursion so u might need something specific lol]
Need: JuliaMono Nerd Font + a few extra symbols only i have
    [thankfully i added the font to the repo]

Install packages: `python -m pip install -r requirements.txt`

Basic command: `python refresher.py /path/to/file.☾`