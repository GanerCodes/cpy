PYTHON_INCLUDED:
    types, ast, collections, itertools, functools
    textwrap, re, unicodedata
    pickle, hashlib, time
    sys, os, io, pathlib, platform, tempfile
    OPTIONAL:
        readline, subprocess…?
        ☾_ENABLE_DEBUG:
            resource, threading, cProfile, pstats, atexit
PYTHON_PIP_OPTIONAL:
    regex, clipboard
    json5, numpy, scipy, pandas, statsmodels
OPTIONAL:
    fontforge